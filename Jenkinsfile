pipeline {
    agent any

    stages {
      

        stage('Echo Version') {
            steps {
                sh 'echo Print Maven version'
                sh 'git -v'
            }
        }
    }
}
